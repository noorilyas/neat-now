

  # Neat now Admin Panel
   This is the admin panel for the Neat Now AI-powered waste management assistant app.
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

